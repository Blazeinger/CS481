# function that adds one to int
func addOne(int x) -> int
{
  -> (x + 1)
}

func main()
{
  # correct declaration initialization
  var int integerOne = addOne(33)
  var bool booleanOne = T || F
  var array<int> arr = new(array<int>, size)

  # error mismatch type declaration initialization
  var int integerTwo = T || F
  var int integerThree = 3 == 5
  var bool booleanTwo = 3 + 5 + 7 - 33
  var bool booleanTwo = addOne(11)
  var array<int> arr = 22
}