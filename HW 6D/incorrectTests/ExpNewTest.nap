func main()
{
  # correct array definition
  var array<int> arr = new(array<int>, 5)

  # error array type doesn't match
  var array<int> arr = new(array<bool>, 5)

  # error array type doesn't match reversed
  var array<bool> arr = new(array<int>, 5)
}
