func num_solutions(int a, int b, int c) -> int
{
   var int d = b * b - 4 * a * c
   if (d < 0) { -> 0 }
   if (d ==0) { -> 1 }
   -> 2
}

func main()
{
  print(array<char>, "Number of solutions: ")
  print(int, num_solutions(1, 2, 1))
}
